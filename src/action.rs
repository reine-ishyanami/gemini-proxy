// 运行代理服务
pub(crate) async fn run_service() {
    log::info!("代理服务运行中...");
    loop {
        
    }
}

// 生成证书
pub(crate) fn generate_ca() {
    log::info!("生成证书中...");
}

// 安装证书
pub(crate) fn install_ca() {
    log::info!("安装证书中...");
}

// 卸载证书
pub(crate) fn uninstall_ca() {
    log::info!("卸载证书中...");
}

// 更新证书
pub(crate) fn update_ca() {
    log::info!("更新证书中...");
}